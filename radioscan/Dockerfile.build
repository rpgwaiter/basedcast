FROM rustlang/rust:nightly

# COPY . /var/radioscan

# WORKDIR /var/radioscan

RUN apt-get update && apt-get install -y libavutil-dev \
    libavformat-dev llvm-7-dev librust-clang-sys-dev clang-7-dev ffmpeg


# RUN nix build .#radioscan && ls result/