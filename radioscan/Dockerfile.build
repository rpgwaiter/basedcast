FROM rustlang/rust:nightly

# COPY . /var/radioscan

# WORKDIR /var/radioscan

RUN apt-get update && apt-get install -y libavutil-dev libgcc-7-dev \
    libavformat-dev llvm-7-dev librust-clang-sys-dev

ENV LIBCLANG_PATH /usr/lib/llvm-7/lib/libclang.so


# RUN nix build .#radioscan && ls result/